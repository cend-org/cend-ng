sudo apt update
sudo apt install nodejs
sudo apt install npm
sudo npm install -g @angular/cli
sudo git clone https://github.com/cend-org/cend-ng.git
ng build
sudo ng serve --port 80 --disable-host-check
